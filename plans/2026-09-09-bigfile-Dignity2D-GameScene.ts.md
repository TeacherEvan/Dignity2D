# Refactor plan — `src/scenes/GameScene.ts` in `TeacherEvan/Dignity2D`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-09T14:46:27.150127+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/Dignity2D` |
| path | `src/scenes/GameScene.ts` |
| size | 43.3 KB (44,356 bytes) |
| lines | 1,450 |
| generated | 2026-09-09T14:46:27.150142+07:00 |

### Top imports (sample)
  - `import Phaser from "phaser";`
  - `import { RoomClient } from "../net/RoomClient";`
  - `import { DEFAULT_SERVER_URL } from "../net/serverApi";`
  - `import { commitCaptureFromTrail } from "../game/capture/captureArea";`
  - `import { movePlayer } from "../game/capture/trailState";`
  - `import {`
  - `import { createEnemyWave } from "../enemies/EnemySpawner";`
  - `import { stepEnemies } from "../enemies/enemyStep";`
  - `import {`
  - `import { damagePlayer } from "../game/player";`

## Structural analysis

**Top-level exports:**
- `resolveSceneLayoutMetrics` (L152)
- `createSceneGameState` (L194)
- `createSceneGameStateForLaunch` (L198)
- `createSceneGameStateForSize` (L239)
- `getScenePerformanceFallbackReason` (L254)
- `advanceGameStateWithDiagnostics` (L260)
- `advanceGameState` (L395)
- `applyRoomStateSyncSnapshot` (L402)
- `makeSceneLaunchData` (L456)
- `makeHudSnapshot` (L460)
- `makeHudDisplayModel` (L485)
- `makePreviewLabel` (L507)
- `makeGameStatusText` (L511)

**Top-level consts:**
- `PLAYER_ID` (L43)
- `PLAYER_SPEED` (L44)
- `ENEMY_RADIUS` (L45)
- `HUD_ENTER_DURATION` (L46)
- `HUD_PULSE_DURATION` (L47)
- `HUD_FRAME_HEIGHT` (L48)
- `HUD_FRAME_CUT` (L49)

**Top-referenced imports:**
- `RoomClient` (4 refs)
- `DEFAULT_SERVER_URL` (3 refs)
- `createEnemyWave` (3 refs)
- `commitCaptureFromTrail` (2 refs)
- `movePlayer` (2 refs)
- `stepEnemies` (2 refs)
- `damagePlayer` (2 refs)
- `spawnProjectile` (2 refs)
- `stepProjectiles` (2 refs)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `Dignity2D`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Confirm `RoomClient` (4 refs) is the right shared utility
- **Target:** `RoomClient` is the most-referenced import in this file (4 uses).
- **Action:** verify whether other dashboard pages also import it; if so, ensure it lives
  in a shared `lib/` or `@/components/ui/` and not in a feature-local path.
- **Acceptance:** document the canonical home for `RoomClient` in `docs/`.
- **Validation:** `rg -l 'RoomClient' src/` — should match ≤3 call sites in
  feature code, more in shared.
- **Evidence:** call-site map.

### OBJ-002 — Move 7 top-level constants to a sibling constants module
- **Target:** Top-level `const <Name> = ...` blocks at L43+
  should live next to the component (e.g. `./constants.ts`).
- **Why:** Constants colocated with the page force every refactor to
  re-validate the data layer. Moving them removes a coupling edge.
- **Acceptance:** `src/scenes/GameScene.ts` no longer defines top-level `const`; imports them instead.
- **Validation:** type-check + lint.
- **Evidence:** `rg -n '^const [A-Z]' src/scenes/GameScene.ts` → 0.

### OBJ-003 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `src/scenes/GameScene.ts` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `src/scenes/GameScene.ts`.
- **Validation:** `rg -n '^import' src/scenes/GameScene.ts` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-004 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `src/scenes/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-005 — Reduce `src/scenes/GameScene.ts` below 725 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `src/scenes/GameScene.ts` shrinks from 1,450 to ≤725 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `src/scenes/GameScene.ts` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `src/scenes/GameScene.ts` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `Dignity2D`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-09-bigfile-Dignity2D-GameScene.ts.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.
